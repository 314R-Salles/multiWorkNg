import { BeanPackagesPipe } from './bean-packages.pipe';

describe('BeanPackagesPipe', () => {
  it('create an instance', () => {
    const pipe = new BeanPackagesPipe();
    expect(pipe).toBeTruthy();
  });
});
